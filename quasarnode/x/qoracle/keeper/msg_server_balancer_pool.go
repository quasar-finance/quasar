package keeper

/*
import (
	"context"
	"fmt"

	"github.com/abag/quasarnode/x/qoracle/types"
	sdk "github.com/cosmos/cosmos-sdk/types"
)

func (k msgServer) BalancerPool(goCtx context.Context, msg *types.MsgBalancerPool) (*types.MsgBalancerPoolResponse, error) {
	ctx := sdk.UnwrapSDKContext(goCtx)

	k.Logger(ctx).Info(fmt.Sprintf("qOracle|BalancerPool|%s\n", msg.String()))

	return &types.MsgBalancerPoolResponse{}, nil
}
*/
