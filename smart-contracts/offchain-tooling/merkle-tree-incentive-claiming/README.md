# Sendblue test server
this app serves as an e2e testing helper. All tests are driven by postman.

## Assumptions
This server assumes that sb-api runs on port 3000 and sb-api assumes sb-test runs on 2999